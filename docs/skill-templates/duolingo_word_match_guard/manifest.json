{
  "name": "duolingo_word_match_guard",
  "description": "Deterministic Duolingo word-match; use describe_ambient_audio first for listen/sound exercises (see SKILL.md).",
  "inputSchema": {
    "type": "object",
    "properties": {
      "target": {
        "type": "string",
        "description": "The target token that must be selected."
      },
      "choices": {
        "type": "array",
        "items": { "type": "string" },
        "description": "Visible clickable labels from current Duolingo word-match grid."
      },
      "allowContainsFallback": {
        "type": "boolean",
        "description": "If true, allow strict contains fallback when exact normalized match is not found."
      }
    },
    "required": ["target", "choices"]
  },
  "outputSchema": {
    "type": "object",
    "properties": {
      "status": { "type": "string" },
      "selected": { "type": "string" },
      "confidence": { "type": "number" },
      "reason": { "type": "string" },
      "matchedIndices": {
        "type": "array",
        "items": { "type": "number" }
      }
    }
  }
}
